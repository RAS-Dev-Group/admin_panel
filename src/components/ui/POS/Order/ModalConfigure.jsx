import Modal from "../../Modal/Modal";

export default function EmailConfigureModal({ initialData, show, handleSubmit, handleClose }) {
  return (
    <Modal show={show} onClose={handleClose}>
      <div>
        
      </div>
    </Modal>
  )
}
