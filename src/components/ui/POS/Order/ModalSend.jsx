import Modal from "../../Modal/Modal";

export default function EmailPDFModal({ initialData, show, handleSubmit, handleClose }) {
  return (
    <Modal show={show} onClose={handleClose}>
      
    </Modal>
  )
}